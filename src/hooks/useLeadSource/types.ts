export type ProspectusOrigin = {
    _id: string
    code: string
    name: string
    description: string
    status: number | null
}

export type ProspectusStatus = {
    _id: string
    code: string
    name: string
    description: string
    status: number | null
}