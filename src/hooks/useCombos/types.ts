export type DocumentType = {
    _id: string
    name: string
    status: number
    operation: string // "comprobante" o "persona"
}
export type Position = {
    _id: string
    name: string
    status: number
}